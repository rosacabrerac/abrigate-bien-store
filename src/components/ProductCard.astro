---
import { Image } from "astro:assets";

interface Props {
  name: string;
  price: number;
  image: string;
  description: string;
  id: number;
  slug: string;
}

const { name, price, image, description, id, slug } = Astro.props;

const formattedPrice = new Intl.NumberFormat("es-AR", {
  style: "currency",
  currency: "ARS",
  maximumFractionDigits: 0,
}).format(price);
---

<article class="group relative flex flex-col">
  <a
    href={`/products/${slug}`}
    class="block relative overflow-hidden aspect-[3/4]"
  >
    <Image
      src={image}
      alt={name}
      inferSize={true}
      format="webp"
      class="w-full h-full object-cover transition-transform duration-700 ease-out group-hover:scale-[1.06]"
    />
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-400 ease-out flex flex-col justify-end p-5"
    >
      <span
        class="block w-full text-center bg-[var(--color-brand)] text-white text-xs font-bold uppercase tracking-widest py-3 translate-y-4 group-hover:translate-y-0 transition-transform duration-400 ease-out"
      >
        Ver Detalles
      </span>
    </div>
  </a>

  <div class="pt-3 pb-1">
    <h2
      class="font-sans text-sm font-semibold text-white leading-snug line-clamp-2"
    >
      {name}
    </h2>
    <p class="font-sans text-[var(--color-brand)] font-bold text-sm mt-1">
      {formattedPrice}
    </p>
  </div>
</article>
